// index of:
let message = "i love only allah";
let position= message.indexOf("allah"); //return index number if item exist otherwise -1
console.log(position);

// includes:
let check = message.includes("love"); //return true is item exist;
console.log(check);

// starts with:
let srt = message.startsWith("I"); //check whether a string start with it or not
console.log(srt);

// ends with:
let end = message.endsWith("h"); //check whether a string end with it or not
console.log(end);